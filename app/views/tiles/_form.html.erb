<%= form_with(model: tile) do |form| %>
  <% if tile.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tile.errors.count, "error") %> prohibited this tile from being saved:</h2>

      <ul>
        <% tile.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :word, style: "display: block" %>
    <%= form.text_field :word %>
  </div>

  <div>
    <%= form.label :color, style: "display: block" %>
    <%= form.text_field :color %>
  </div>

  <div>
    <%= form.label :solution_set, style: "display: block" %>
    <%= form.number_field :solution_set %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
